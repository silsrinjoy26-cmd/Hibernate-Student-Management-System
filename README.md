# Hibernate-Student-Management-System
A Java-based Student Management System built using Hibernate ORM. Implements CRUD operations for student and related entities. Demonstrates Hibernate entity mapping and database relationships. Uses MySQL for persistent data storage. Built with Maven and JUnit for testing.
